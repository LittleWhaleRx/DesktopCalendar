<Window x:Class="DesktopCalendar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="桌面台历"
        Width="920"
        Height="590"
        MinWidth="720"
        MinHeight="480"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        WindowStartupLocation="Manual"
        FontFamily="Microsoft YaHei UI"
        Foreground="#F4F8FA"
        SourceInitialized="Window_SourceInitialized"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        LocationChanged="Window_PositionChanged"
        SizeChanged="Window_PositionChanged">
    <Window.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="32"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="Foreground" Value="#F4F8FA"/>
            <Setter Property="Background" Value="#1CFFFFFF"/>
            <Setter Property="BorderBrush" Value="#30FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2EFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#42FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="#F4F8FA"/>
            <Setter Property="Background" Value="#1A000000"/>
            <Setter Property="BorderBrush" Value="#22FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#DCE8EE"/>
            <Setter Property="Margin" Value="12,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border x:Name="RootChrome"
            CornerRadius="8"
            Background="#D0101418"
            BorderBrush="#38FFFFFF"
            BorderThickness="1"
            Padding="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="28"/>
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource ToolbarButtonStyle}" Content="‹" Click="PreviousMonth_Click" ToolTip="上个月"/>
                    <Button Style="{StaticResource ToolbarButtonStyle}" Content="今天" Click="Today_Click"/>
                    <Button Style="{StaticResource ToolbarButtonStyle}" Content="›" Click="NextMonth_Click" ToolTip="下个月"/>
                    <TextBlock x:Name="MonthTitle"
                               Margin="14,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="20"
                               FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="DesktopModeCheck" Content="嵌入桌面" Checked="DesktopModeCheck_Changed" Unchecked="DesktopModeCheck_Changed"/>
                    <CheckBox x:Name="LockCheck" Content="锁定位置" Checked="LockCheck_Changed" Unchecked="LockCheck_Changed"/>
                    <Button Style="{StaticResource ToolbarButtonStyle}" Content="—" Click="Minimize_Click" ToolTip="最小化"/>
                    <Button Style="{StaticResource ToolbarButtonStyle}" Content="×" Click="Close_Click" ToolTip="关闭"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.08*"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="34"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <UniformGrid Grid.Row="0" Rows="1" Columns="7">
                        <TextBlock Text="日" HorizontalAlignment="Center" Foreground="#9FB2BC"/>
                        <TextBlock Text="一" HorizontalAlignment="Center" Foreground="#9FB2BC"/>
                        <TextBlock Text="二" HorizontalAlignment="Center" Foreground="#9FB2BC"/>
                        <TextBlock Text="三" HorizontalAlignment="Center" Foreground="#9FB2BC"/>
                        <TextBlock Text="四" HorizontalAlignment="Center" Foreground="#9FB2BC"/>
                        <TextBlock Text="五" HorizontalAlignment="Center" Foreground="#9FB2BC"/>
                        <TextBlock Text="六" HorizontalAlignment="Center" Foreground="#9FB2BC"/>
                    </UniformGrid>

                    <UniformGrid x:Name="CalendarGrid" Grid.Row="1" Rows="6" Columns="7"/>
                </Grid>

                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="126"/>
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock x:Name="SelectedDateTitle" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock x:Name="AnniversaryTodayLabel" Margin="0,4,0,0" Foreground="#A9D7E8"/>
                    </StackPanel>

                    <GroupBox Grid.Row="1" Header="工作计划" Foreground="#DCE8EE" Margin="0,0,0,10">
                        <TextBox x:Name="PlanBox"
                                 Style="{StaticResource InputBoxStyle}"
                                 TextChanged="PlanBox_TextChanged"/>
                    </GroupBox>

                    <GroupBox Grid.Row="2" Header="当天完成" Foreground="#DCE8EE" Margin="0,0,0,10">
                        <TextBox x:Name="DoneBox"
                                 Style="{StaticResource InputBoxStyle}"
                                 TextChanged="DoneBox_TextChanged"/>
                    </GroupBox>

                    <GroupBox Grid.Row="3" Header="纪念日" Foreground="#DCE8EE">
                        <Grid Margin="0,4,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="AnniversaryInput"
                                     Grid.Column="0"
                                     Height="30"
                                     Padding="8,4"
                                     VerticalContentAlignment="Center"
                                     Background="#1A000000"
                                     Foreground="#F4F8FA"
                                     BorderBrush="#22FFFFFF"
                                     CaretBrush="#FFFFFF"/>
                            <Button Grid.Column="1"
                                    Style="{StaticResource ToolbarButtonStyle}"
                                    Content="添加"
                                    Click="AddAnniversary_Click"/>
                            <Button Grid.Column="2"
                                    Style="{StaticResource ToolbarButtonStyle}"
                                    Content="删除"
                                    Click="DeleteAnniversary_Click"/>
                            <ListBox x:Name="AnniversaryList"
                                     Grid.Row="1"
                                     Grid.ColumnSpan="3"
                                     Margin="0,8,0,0"
                                     Background="Transparent"
                                     Foreground="#F4F8FA"
                                     BorderThickness="0"/>
                        </Grid>
                    </GroupBox>
                </Grid>
            </Grid>

            <Grid Grid.Row="2" Margin="2,8,2,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="透明度" Foreground="#9FB2BC" VerticalAlignment="Center"/>
                <Slider x:Name="OpacitySlider"
                        Grid.Column="1"
                        Margin="10,0,0,0"
                        Minimum="55"
                        Maximum="95"
                        ValueChanged="OpacitySlider_ValueChanged"/>
                <TextBlock x:Name="StatusText"
                           Grid.Column="2"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Foreground="#8EA3AD"/>
            </Grid>
        </Grid>
    </Border>
</Window>
